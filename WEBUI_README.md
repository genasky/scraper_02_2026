# WebUI для Search Engines Scraper

Современный веб-интерфейс для мульти-поискового парсера с поддержкой различных поисковых систем.

## 🚀 Запуск

1. **Активируйте виртуальное окружение:**
   ```bash
   source venv/bin/activate
   ```

2. **Запустите веб-интерфейс:**
   ```bash
   python webui.py
   ```

3. **Откройте в браузере:**
   ```
   http://localhost:5001
   ```

## ✨ Возможности

### 🔍 Поиск
- **Мульти-поисковый поиск**: одновременный поиск в нескольких системах
- **Поддерживаемые движки**: Bing, Yahoo, Startpage, Aol, DuckDuckGo, Ecosia
- **Кастомизация**: выбор количества страниц, фильтров, прокси

### 📊 Результаты
- **Удобное отображение**: карточки с заголовками, ссылками и сниппетами
- **Экспорт**: выгрузка результатов в JSON или CSV
- **Фильтрация**: удаление дубликатов, фильтры по URL/тексту/хосту

### 🎨 Интерфейс
- **Современный дизайн**: градиентный фон, карточки, анимации
- **Адаптивность**: работает на десктопе и мобильных устройствах
- **Темная тема**: автоматическое переключение при системной темной теме

## 🛠️ API Endpoints

### POST /search
Выполняет поиск по указанным параметрам.

**Request:**
```json
{
  "query": "python programming",
  "engines": ["bing", "yahoo"],
  "pages": 2,
  "proxy": "http://ip:port",
  "ignore_duplicates": true,
  "filter": "title"
}
```

**Response:**
```json
{
  "success": true,
  "results": [...],
  "total": 20,
  "query": "python programming",
  "engines": ["bing", "yahoo"]
}
```

### POST /export
Экспортирует результаты в указанный формат.

**Request:**
```json
{
  "results": [...],
  "format": "json"
}
```

**Response:**
```json
{
  "success": true,
  "filename": "search_results.json",
  "download_url": "/download/search_results.json"
}
```

### GET /download/<filename>
Скачивает экспортированный файл.

## 🎯 Использование

1. **Введите запрос** в поле поиска
2. **Выберите поисковые системы** (можно несколько)
3. **Настройте параметры**:
   - Количество страниц (1-10)
   - Фильтр результатов
   - Прокси (опционально)
   - Игнорировать дубликаты
4. **Нажмите "Найти"**
5. **Просмотрите результаты** и экспортируйте при необходимости

## ⌨️ Горячие клавиши

- **Ctrl/Cmd + Enter**: выполнить поиск
- **Escape**: скрыть результаты

## 📁 Структура файлов

```
├── webui.py              # Flask приложение
├── templates/
│   └── index.html        # HTML шаблон
├── static/
│   ├── css/
│   │   └── style.css     # Стили
│   └── js/
│       └── script.js     # JavaScript
└── exports/              # Папка для экспорта
```

## 🔧 Технологии

- **Backend**: Flask, Flask-CORS
- **Frontend**: HTML5, CSS3, JavaScript (ES6+)
- **Icons**: Font Awesome 6.0
- **Styling**: CSS Grid, Flexbox, CSS Variables
- **Features**: Async/await, Fetch API, Local Storage

## 🌐 Особенности

- **Progressive Enhancement**: работает даже без JavaScript
- **Responsive Design**: адаптивный дизайн для всех устройств
- **Accessibility**: семантическая HTML5 разметка
- **Performance**: оптимизированные запросы и кэширование
- **Security**: защита от XSS, валидация данных

## 📱 Поддержка

- Chrome/Chromium 60+
- Firefox 55+
- Safari 12+
- Edge 79+

## 🐛 Отладка

Веб-интерфейс работает в debug-режиме. Для продакшена измените:

```python
app.run(debug=False, host='0.0.0.0', port=5001)
```

---

**Готово к использованию!** 🎉
